apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clusteradcsissuers.adcs.certmanager.lcwsre.io
spec:
  group: adcs.certmanager.lcwsre.io
  names:
    kind: ClusterAdcsIssuer
    listKind: ClusterAdcsIssuerList
    plural: clusteradcsissuers
    singular: clusteradcsissuer
  scope: Cluster
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          description: ClusterAdcsIssuer is the Schema for the clusteradcsissuers API
          type: object
          properties:
            apiVersion:
              description: APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              description: ClusterAdcsIssuerSpec defines the desired state of ClusterAdcsIssuer
              type: object
              required:
                - credentialsRef
                - url
              properties:
                authMode:
                  description: Authentication mode for the ADCS server. Supported values are "ntlm" (default) and "basic".
                  type: string
                  enum:
                    - ntlm
                    - basic
                caBundle:
                  description: CABundle is a PEM encoded TLS certificate to use to verify connections to the ADCS server.
                  type: string
                  format: byte
                credentialsRef:
                  description: CredentialsRef is a reference to a Secret containing the username and password for the ADCS server.
                  type: object
                  required:
                    - name
                  properties:
                    name:
                      description: Name of the referent.
                      type: string
                retryInterval:
                  description: How often to retry in case of communication errors (in time.ParseDuration() format). Default 1 hour.
                  type: string
                statusCheckInterval:
                  description: How often to check for request status in the server (in time.ParseDuration() format). Default 6 hours.
                  type: string
                url:
                  description: URL is the base URL for the ADCS instance.
                  type: string
            status:
              description: ClusterAdcsIssuerStatus defines the observed state of ClusterAdcsIssuer
              type: object
